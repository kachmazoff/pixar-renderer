cmake_minimum_required(VERSION 3.10)

project(Pixar-Renderer VERSION 0.1) # set the project name and version
add_executable(main ${PROJECT_SOURCE_DIR}/src/main.cpp) # Создает исполняемый файл с именем main из исходника main.cpp
